<%= form_for(@store) do |f| %>
  <% if @store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store.errors.count, "error") %> prohibited this store stock from being saved:</h2>

      <ul>
      <% @store.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :in_stock %><br>
    <%= f.check_box :in_stock %>
  </div>
  <div class="field">
    <%= f.label :supplier %><br>
    <%= f.text_area :supplier %>
  </div>
  <div class="field">
    <%= f.label :delivery_date %><br>
    <%= f.date_select(:delivery_date, start_year: Date.current.year)  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
